# Use this transformer to configure your SAS deployment to utilize
# SSL enabled PostgreSQL

apiVersion: builtin
kind: ConfigMapGeneator
metadata:
  name: postgres-enable-ssl
literals:
  - DATABASE_SSL_ENABLED="true"
behavior: merge