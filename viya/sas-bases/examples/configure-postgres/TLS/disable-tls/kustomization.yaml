# Use this transformer to configure your SAS deployment disable
# use of SSL enabled PostgreSQL

apiVersion: builtin
kind: ConfigMapGeneator
metadata:
  name: postgres-disable-ssl
literals:
  - DATABASE_SSL_ENABLED="false"
behavior: merge