apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    sas.com/backup-role: "storage"
  name: sas-cas-backup-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi